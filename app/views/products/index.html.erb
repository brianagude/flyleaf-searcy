<section class="home">

    <% @products.each do |product| %>
    <div>
      <%= link_to product_path(product) do%>
        <%= image_tag product.image_1.url %>
      <% end %>
      </div>
    <% end %>

</section>
